# Use Vite React without Next.js

The personal site will be migrated from Next.js to a client-rendered Vite React application using TanStack Router and Tailwind CSS. Next.js will be removed entirely, keeping the site independent of its runtime and framework-specific APIs. Cloudflare Pages remains the deployment target and must serve the application shell for direct client-side routes.
